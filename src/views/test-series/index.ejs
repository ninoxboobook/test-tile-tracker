<%- contentFor('body') %>

<div class="py-6">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center">
      <h1 class="text-2xl font-semibold text-gray-900">Test Series</h1>
      <button type="button" onclick="window.location.href='/test-series/new'" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-clay-600 hover:bg-clay-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-clay-500">
        Create New Series
      </button>
    </div>

    <div class="mt-8">
      <% if (series && series.length > 0) { %>
        <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
          <% series.forEach(function(s) { %>
            <div class="bg-white overflow-hidden shadow rounded-lg divide-y divide-gray-200">
              <div class="p-5">
                <div class="flex items-center justify-between">
                  <h3 class="text-lg leading-6 font-medium text-gray-900">
                    <%= s.name %>
                  </h3>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-clay-100 text-clay-800">
                    <%= s.TestTiles ? s.TestTiles.length : 0 %> tiles
                  </span>
                </div>
                <div class="mt-2">
                  <p class="text-sm text-gray-500">
                    <%= s.description || 'No description provided' %>
                  </p>
                </div>
                <div class="mt-4">
                  <a href="/test-series/<%= s.id %>" class="text-sm font-medium text-clay-600 hover:text-clay-500">
                    View Series <span aria-hidden="true">&rarr;</span>
                  </a>
                </div>
              </div>
              <div class="px-5 py-3 bg-gray-50">
                <div class="text-xs text-gray-500">
                  Created <%= new Date(s.createdAt).toLocaleDateString() %>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="text-center py-12">
          <h3 class="mt-2 text-sm font-medium text-gray-900">No test series</h3>
          <p class="mt-1 text-sm text-gray-500">Get started by creating a new test series.</p>
          <div class="mt-6">
            <button type="button" onclick="window.location.href='/test-series/new'" class="inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-clay-600 hover:bg-clay-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-clay-500">
              Create New Series
            </button>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</div>
